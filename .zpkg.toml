[package]
org = "evento-globolo"
name = "evento-globolo-libs"
version = "0.1.0"
description = "Shared libraries for Evento Globolo"
license = "MIT"
language = "rust"

[package.repository]
vcs = "git"
url = "https://github.com/evento-globolo/evento-globolo-libs"

[install]
dir = ".vendor/.zed"

[dependencies]
"evento-globolo/evgl-interfaces" = "^0.1.0"

[publish]
include_readme = true
tag_format = "v{version}"
exclude = [".env", ".env.*", "node_modules/**", "target/**", "dist/**", "*.log", ".vendor/.zed/**"]
